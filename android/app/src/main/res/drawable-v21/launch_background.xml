<?xml version="1.0" encoding="utf-8"?>
<!-- Modify this file to customize your launch splash screen -->
<!-- Prime is dark-first unconditionally (no light theme exists — see
     lib/app.dart's `themeMode: ThemeMode.dark`), so the launch splash is
     always dark too, regardless of the system's own light/dark setting
     (this deliberately does not use the theme-relative
     `?android:colorBackground`, which would still be white on a
     light-system-theme device). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/ic_launcher_background" />

    <!-- You can insert your own image assets here -->
    <!-- <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/launch_image" />
    </item> -->
</layer-list>
